# LearnMatrix System Architecture

## 📐 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          CLIENT LAYER (Browser)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────────────────────┐         ┌──────────────────────────┐          │
│  │   Student Portal         │         │   Teacher Portal         │          │
│  ├──────────────────────────┤         ├──────────────────────────┤          │
│  │ • Dashboard              │         │ • Analytics              │          │
│  │ • Focus Sessions         │         │ • Student Roster         │          │
│  │ • Performance Charts     │         │ • Question Effectiveness │          │
│  │ • Doubt Submission       │         │ • Doubt Frequency        │          │
│  │ • Trophy Case            │         │ • Assignment Management  │          │
│  │ • Activity Logging       │         │ • Engagement Metrics     │          │
│  └──────────────────────────┘         └──────────────────────────┘          │
│                                                                              │
│  ┌──────────────────────────┐         ┌──────────────────────────┐          │
│  │   Authentication Pages   │         │   Visualizations         │          │
│  ├──────────────────────────┤         ├──────────────────────────┤          │
│  │ • Login Form             │         │ • Chart.js Graphs        │          │
│  │ • Registration Form      │         │ • Line Charts            │          │
│  │ • Role Selection         │         │ • Radar Charts           │          │
│  │ • Session Management     │         │ • Bar Charts             │          │
│  │ • Error Handling         │         │ • Pie Charts             │          │
│  └──────────────────────────┘         └──────────────────────────┘          │
│                                                                              │
└────────────────┬────────────────────────────────┬───────────────────────────┘
                 │                                │
                 │ HTTP/HTTPS (JSON/HTML)         │
                 │                                │
┌────────────────▼────────────────────────────────▼───────────────────────────┐
│                        WEB SERVER LAYER (Flask)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Middleware Layer                             │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │ • Session Management    • Error Handling    • Input Validation      │   │
│  │ • CORS Headers         • Logging            • Authentication        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌────────────────┐   │
│  │ Authentication       │  │ Student Routes       │  │ Teacher Routes │   │
│  ├──────────────────────┤  ├──────────────────────┤  ├────────────────┤   │
│  │ • /register          │  │ • /dashboard         │  │ • /dashboard   │   │
│  │ • /login             │  │ • /api/performance   │  │ • /api/roster  │   │
│  │ • /logout            │  │ • /weakness-topics   │  │ • /analysis    │   │
│  │                      │  │ • /focus-session     │  │ • /doubts      │   │
│  │                      │  │ • /log-activity      │  │ • /assign      │   │
│  │                      │  │ • /submit-doubt      │  │                │   │
│  └──────────────────────┘  └──────────────────────┘  └────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                  Business Logic Layer                              │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │ • Weakness Topic Calculation   • Engagement Score Calculation      │   │
│  │ • Focus Session Generation     • Achievement Awarding              │   │
│  │ • Activity Logging             • Question Effectiveness Analysis   │   │
│  │ • Doubt Queue Management       • Performance Analytics             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└────────────────┬────────────────────────────────┬───────────────────────────┘
                 │                                │
                 │ SQL Queries                    │
                 │ Parameterized Statements       │
                 │                                │
┌────────────────▼────────────────────────────────▼───────────────────────────┐
│                        DATABASE LAYER (MySQL)                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ Core Tables                                                         │   │
│  ├─────────────────────────────────────────────────────────────────────┤   │
│  │ • Users ─┐                                                          │   │
│  │          ├── Assignments ─┬─── Results ─┬─── ActivityLog           │   │
│  │ • Exams ─┤                ├─── Doubts   └─── Achievements          │   │
│  │          └── Questions ───┘                                         │   │
│  │                                                                     │   │
│  │ ┌─────────────────┐      ┌──────────────────┐                      │   │
│  │ │ Relationships   │      │ Constraints      │                      │   │
│  │ ├─────────────────┤      ├──────────────────┤                      │   │
│  │ │ • FK Chains     │      │ • Primary Keys   │                      │   │
│  │ │ • Cascading     │      │ • Unique Fields  │                      │   │
│  │ │ • Left Joins    │      │ • NOT NULL       │                      │   │
│  │ │ • Aggregations  │      │ • Enums          │                      │   │
│  │ └─────────────────┘      └──────────────────┘                      │   │
│  │                                                                     │   │
│  │ ┌──────────────────────────────────────────────────────┐           │   │
│  │ │ Indexes (Performance Optimization)                 │           │   │
│  │ ├──────────────────────────────────────────────────────┤           │   │
│  │ │ • idx_username, idx_email              (Users)      │           │   │
│  │ │ • idx_exam_topic, idx_topic            (Questions)  │           │   │
│  │ │ • idx_student_results, idx_timestamp   (Results)    │           │   │
│  │ │ • idx_user_timestamp                   (ActivityLog)│           │   │
│  │ │ • idx_student_achievements             (Achievements)           │   │
│  │ └──────────────────────────────────────────────────────┘           │   │
│  │                                                                     │   │
│  │ ┌──────────────────────────────────────────────────────┐           │   │
│  │ │ JSON Storage (Flexible Data)                        │           │   │
│  │ ├──────────────────────────────────────────────────────┤           │   │
│  │ │ • Questions.Options      → Multiple choice options  │           │   │
│  │ │ • ActivityLog.Details    → Custom metadata          │           │   │
│  │ └──────────────────────────────────────────────────────┘           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Student Registration & Login Flow

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ 1. Navigate /register
       │
┌──────▼──────────────────────────┐
│  Display Registration Form       │
│  (HTML Template)                 │
└──────┬──────────────────────────┘
       │ 2. Fill Form & Submit
       │
┌──────▼──────────────────────────┐
│  POST /register (JSON)           │
│  {username, email, password,     │
│   role, first_name, last_name}   │
└──────┬──────────────────────────┘
       │ 3. Validate Input
       │
┌──────▼──────────────────────────┐
│  Flask: register() Route         │
│  • Check uniqueness              │
│  • Hash password (bcrypt)        │
│  • Validate email format         │
└──────┬──────────────────────────┘
       │ 4. INSERT User
       │
┌──────▼──────────────────────────┐
│  MySQL: Users Table              │
│  INSERT INTO Users (...)         │
└──────┬──────────────────────────┘
       │ 5. Success Response
       │
┌──────▼──────────────────────────┐
│  Return 201 Created              │
│  Message: "Registration OK"      │
└──────┬──────────────────────────┘
       │ 6. Redirect to /login
       │
┌──────▼──────────────────────────┐
│  Browser: Login Page             │
│  User enters credentials         │
└──────┬──────────────────────────┘
       │ 7. POST /login (JSON)
       │
┌──────▼──────────────────────────┐
│  Flask: login() Route            │
│  • SELECT User WHERE username    │
│  • verify_password()             │
│  • Create session                │
└──────┬──────────────────────────┘
       │ 8. Set Session Cookie
       │
┌──────▼──────────────────────────┐
│  Return 200 OK                   │
│  {redirect: /student/dashboard}  │
└──────┬──────────────────────────┘
       │ 9. Redirect
       │
┌──────▼──────────────────────────┐
│  Student Dashboard               │
│  (Session active)                │
└─────────────────────────────────┘
```

### 2. Student Weakness Drill Flow

```
┌──────────────────────────────┐
│ Student Dashboard            │
│ "Start Focus Session" Button  │
└──────────────┬───────────────┘
               │ Click
               │
┌──────────────▼───────────────┐
│ GET /api/student/            │
│     weakness-topics           │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Flask: get_weakness_topics()  │
│ SELECT r.Topic,               │
│   AVG(r.Percentage)           │
│ FROM Results r                │
│ GROUP BY Topic                │
│ ORDER BY AVG ASC              │
│ LIMIT 2                       │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ MySQL Results Table Query     │
│ Return 2 weakest topics       │
│ Physics: 62.5%                │
│ Chemistry: 58.3%              │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Return JSON Response          │
│ {weaknessTopics: [Physics,    │
│  Chemistry]}                  │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ JavaScript: startFocusSession()
│ Calls /student/focus-session/ │
│ with exam_id                  │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ GET /student/focus-session/1  │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Flask: focus_session()        │
│ SELECT * FROM Questions       │
│ WHERE ExamID=1                │
│ AND Topic IN (Physics,        │
│              Chemistry)       │
│ ORDER BY RAND()               │
│ LIMIT 10                      │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Return 10 Questions JSON      │
│ [questionId, text, options]   │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ Display 10 Questions          │
│ Student answers them          │
│ Submit answers                │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ POST /api/log-activity        │
│ {activity_type: "FocusSession"│
│  duration: 1800}              │
└──────────────┬───────────────┘
               │
┌──────────────▼───────────────┐
│ INSERT ActivityLog            │
│ Check & Award Achievements    │
└──────────────────────────────┘
```

### 3. Teacher Analysis Flow

```
┌──────────────────────────────────┐
│ Teacher Dashboard                │
│ Select Exam → View Effectiveness │
└──────────────┬───────────────────┘
               │ POST /api/teacher/analysis/
               │       question-effectiveness
               │
┌──────────────▼───────────────────┐
│ Flask: question_effectiveness()   │
│ • Verify teacher owns exam        │
│ • Complex JOIN query              │
└──────────────┬───────────────────┘
               │
┌──────────────▼───────────────────┐
│ SQL Query:                        │
│ SELECT q.*, AVG(CompletionTime)  │
│ FROM Questions q                  │
│ LEFT JOIN Results r               │
│ ON r.Topic = q.Topic              │
│ WHERE q.ExamID = ? AND            │
│       r.StudentID IN (...)        │
│ GROUP BY q.QuestionID             │
│ ORDER BY SuccessRate ASC          │
└──────────────┬───────────────────┘
               │
┌──────────────▼───────────────────┐
│ MySQL Processing:                │
│ • Join 4 tables                   │
│ • Aggregate with GROUP BY         │
│ • Calculate success rates         │
│ • Classify difficulty             │
└──────────────┬───────────────────┘
               │
┌──────────────▼───────────────────┐
│ Return Analysis JSON:             │
│ [{questionId, topic, subTopic,    │
│   avgCompletionTime,              │
│   totalAttempts,                  │
│   successRate,                    │
│   difficulty}]                    │
└──────────────┬───────────────────┘
               │
┌──────────────▼───────────────────┐
│ Display Effectiveness Table       │
│ Color-code by difficulty          │
│ Sort by success rate              │
└──────────────────────────────────┘
```

---

## 🎯 Component Interaction Diagram

```
                    ┌─────────────────┐
                    │  User (Browser) │
                    └────────┬────────┘
                             │
                ┌────────────┼────────────┐
                │            │            │
        ┌───────▼────────┐   │  ┌────────▼──────┐
        │ Student Portal │   │  │ Teacher Portal │
        ├────────────────┤   │  ├───────────────┤
        │ • View Data    │   │  │ • View Data   │
        │ • Log Activity │   │  │ • Create Test │
        │ • Submit Doubt │   │  │ • Analyze     │
        └───────┬────────┘   │  └────────┬──────┘
                │            │           │
                │  ┌─────────▼─────────┐ │
                │  │   Flask Router    │ │
                │  │ (Session Manager) │ │
                │  └─────────┬─────────┘ │
                │            │           │
                │        ┌───▼────────────┤
                │        │ Authentication │
                │        │ Validation     │
                │        │ Business Logic │
                │        └───┬────────────┘
                │            │
                └────────┬───┘
                         │
                 ┌───────▼────────┐
                 │  DB Connection │
                 │  (mysql.conn)  │
                 └───────┬────────┘
                         │
          ┌──────────────┼──────────────┐
          │              │              │
    ┌─────▼────┐  ┌─────▼────┐  ┌─────▼────┐
    │  SELECT  │  │  INSERT  │  │  UPDATE  │
    │ Queries  │  │ Statements   │ Statements
    └──────────┘  └──────────┘  └──────────┘
          │              │              │
          └──────────────┼──────────────┘
                         │
          ┌──────────────▼──────────────┐
          │   MySQL Database            │
          │  (8 Tables with Indexes)    │
          └─────────────────────────────┘
```

---

## 🔐 Authentication & Session Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     Authentication Flow                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Step 1: User Login                                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ POST /login {username, password}                    │  │
│  │ → Flask receives request                            │  │
│  │ → Query Users table WHERE username = ?              │  │
│  │ → verify_password(provided_password, db_hash)       │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Step 2: Session Creation                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ IF password correct:                                │  │
│  │ → session['user_id'] = user_id                      │  │
│  │ → session['username'] = username                    │  │
│  │ → session['role'] = role                            │  │
│  │ → Set-Cookie: session_id=xxxx (encrypted)           │  │
│  │ → Response: {redirect: /student/dashboard}          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Step 3: Protected Route Access                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ GET /student/dashboard                              │  │
│  │ → Browser sends Cookie: session_id=xxxx             │  │
│  │ → @login_required decorator checks session          │  │
│  │ IF 'user_id' in session:                            │  │
│  │   → Allow route execution                           │  │
│  │ ELSE:                                               │  │
│  │   → redirect('/login')                              │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Step 4: Role-Based Access                                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ GET /teacher/dashboard                              │  │
│  │ → @role_required('Teacher') decorator               │  │
│  │ IF session['role'] == 'Teacher':                    │  │
│  │   → Allow route execution                           │  │
│  │ ELSE:                                               │  │
│  │   → Return 403 Forbidden                            │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  Step 5: Logout                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ GET /logout                                         │  │
│  │ → session.clear()                                   │  │
│  │ → Delete all session data                           │  │
│  │ → Redirect: /login                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Key Calculations & Metrics

### 1. Weakness Topic Identification

```
SQL Query:
  SELECT Topic, AVG(Percentage) as AvgScore
  FROM Results
  WHERE StudentID = ?
  GROUP BY Topic
  ORDER BY AvgScore ASC
  LIMIT 2

Result: 2 topics with lowest average percentage scores
Example: Physics (62.5%), Chemistry (58.3%)
```

### 2. Engagement Score Calculation

```
Formula:
  EngagementScore = (StudyHours × 0.3) + 
                    (CompletionRate × 0.4) + 
                    (TrophyCount × 0.3)

Components:
  • StudyHours = SUM(Duration) / 3600 from ActivityLog
  • CompletionRate = COUNT(Completed) / COUNT(Total) × 100%
  • TrophyCount = COUNT(*) from Achievements

Range: 0-100 (typically)
Classification:
  • High: ≥ 70
  • Medium: 40-69
  • Low: < 40
```

### 3. Question Success Rate

```
Formula:
  SuccessRate = COUNT(Percentage ≥ 50%) / COUNT(Total) × 100%

Components:
  • Numerator: Results with score ≥ 50%
  • Denominator: All Results containing the question's topic
  
Difficulty Classification:
  • Easy: ≥ 70% (students understand)
  • Medium: 30-70% (moderate difficulty)
  • Hard: < 30% (challenging for students)
```

### 4. Achievement Criteria

```
Focused Learner:
  SUM(Duration WHERE ActivityType='FocusSession') >= 18000 seconds (5 hours)
  AND Timestamp >= NOW() - INTERVAL 7 DAY

High Scorer:
  EXISTS(SELECT * FROM Results 
         WHERE StudentID=? AND Percentage >= 90)

Consistent:
  COUNT(DISTINCT DATE(Timestamp)) >= 7 days
  WHERE UserID=? AND Timestamp >= NOW() - INTERVAL 7 DAY

Problem Solver:
  COUNT(DoubtID WHERE StudentID=? AND Status='Cleared') >= 5
```

---

## 🔄 Request/Response Cycle

### Typical API Request Cycle (200ms average)

```
T+0ms    Client sends HTTP POST request
         ├─ Browser processes DNS
         ├─ Establishes TCP connection
         └─ Sends HTTP headers + payload

T+10ms   Network transit (variable)

T+20ms   Flask receives request
         ├─ URL routing (1ms)
         ├─ Middleware processing (2ms)
         └─ Route handler execution

T+25ms   Validation & business logic (5-10ms)
         ├─ Input validation
         ├─ Session verification
         └─ Authorization checks

T+35ms   Database query execution (50-100ms)
         ├─ Connection pool retrieval
         ├─ SQL query preparation
         ├─ MySQL query execution
         └─ Result set marshaling

T+100ms  Response preparation (5-10ms)
         ├─ JSON serialization
         ├─ Error handling
         └─ Response headers

T+110ms  Send response to client
         └─ HTTP 200/201/400/500 + body

T+120ms  Client receives response
         ├─ Parse JSON
         ├─ Update DOM
         └─ Render UI

T+150ms  Complete (typical for interactive request)
```

---

## 🎓 Technology Stack Justification

```
┌─────────────────────────────────────────────────────────┐
│ Choice              │ Reason                           │
├─────────────────────────────────────────────────────────┤
│ Python/Flask        │ Rapid development, readable code │
│ MySQL               │ ACID compliance, relational,     │
│                     │ proven at scale                  │
│ Bcrypt              │ Secure password hashing standard │
│ Chart.js            │ Lightweight, responsive charts   │
│ HTML5/CSS3          │ Standards-based, semantic        │
│ Dark Mode           │ Modern, reduces eye strain       │
│ Responsive Design   │ Mobile-first, accessibility      │
│ Gunicorn/Nginx      │ Production-grade, scalable       │
└─────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability Considerations

```
Current Setup (Single Server):
  • Users: 0-1,000
  • QPS: 0-100 requests/second
  • Database: Local MySQL

Scale to 10,000 Users:
  ├─ Horizontal Scaling
  │  ├─ Multiple Flask instances (Gunicorn workers)
  │  ├─ Load balancer (Nginx, HAProxy)
  │  └─ Sticky sessions or Redis session store
  │
  ├─ Database Optimization
  │  ├─ Read replicas for analytics
  │  ├─ Connection pooling (5-10 connections)
  │  └─ Query caching (Redis)
  │
  └─ Frontend Optimization
     ├─ CDN for static assets
     ├─ Gzip compression
     └─ Browser caching

Scale to 100,000+ Users:
  ├─ Microservices architecture
  ├─ Message queue (RabbitMQ, Celery)
  ├─ Database sharding
  ├─ Elasticsearch for search
  └─ Elasticsearch/Kibana for logging
```

---

**Architecture Last Updated**: November 18, 2025  
**Documentation Version**: 1.0.0
